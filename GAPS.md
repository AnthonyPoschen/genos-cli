# Gaps

A human must create a personal access token, or run `genos auth login`, against a real Genos deployment that uses Clerk. This repository cannot finish that login by itself.

This repository does not do Discord work.
